#include <bits/stdc++.h>
#pragma GCC optimize("O3,Ofast,fast-math,no-stack-protector,unroll-loops")
using namespace std;



int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);cout.tie(0);
	int i, j, k, p, q, r, a, b, c, m, n;
	while (cin >> )
}